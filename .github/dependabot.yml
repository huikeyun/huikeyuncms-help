version: 2
updates:
    # 为 npm 启用版本更新
    - package-ecosystem: "npm"
        # Look for `package.json` and `lock` files in the `root` directory
      directory: "/"
        # 每天（工作日）检查 npm 注册表以获取更新
      schedule:
          interval: "daily"
